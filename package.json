{
  "name": "aisle-rush",
  "version": "1.0.0",
  "private": true,
  "description": "A mobile-first, after-hours shopping cart racing game.",
  "license": "MIT",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "develop": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs --dist",
    "test": "node --check game.js && node --check scripts/build.mjs && node --check scripts/serve.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
