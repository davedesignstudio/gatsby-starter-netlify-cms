{
  "name": "cart-dash",
  "description": "A touch-first shopping-cart arcade racing game",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "build": "node scripts/build.js",
    "test": "node --check js/cart-dash.js && node --check service-worker.js"
  }
}
